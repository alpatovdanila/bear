import { defineConfig } from 'vite';

// base './' — относительные пути: работает и локально, и на GitHub Pages
// (https://alpatovdanila.github.io/bear/)
export default defineConfig({
  base: './',
});
